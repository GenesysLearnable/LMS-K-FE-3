import { useState, useEffect } from "react";

const Fetch = () => {
	// const [courseList, setCourseList] = useState([]);
	// useEffect(() => {
	// 	fetch("https://lms-k-be-3.onrender.com/api/v1/admin/get-courses")
	// 		.then((res) => {
	// 			return res.json();
	// 		})
	// 		.then((data) => {
	// 			console.log(data);
	// 			setCourseList(data);
	// 		});
	// }, []);
	// return (
	// 	<div>
	// 		<h1>List of courses</h1>
	// 		{courseList.map((course) => (
	// 			<div key={course.id}>
	// 				<h2>{course.title}</h2>
	// 				<p>{course.description}</p>
	// 			</div>
	// 		))}
	// 	</div>
	// );
};
export default Fetch;
